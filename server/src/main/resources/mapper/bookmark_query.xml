<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.ssafy.happyhouse.dao.BookMarkDao">

	<select id="getMyBookmarkHouseDetailList" parameterType="string" resultType="com.ssafy.happyhouse.dto.BookMarkResultDto">
		select user_id, house_no
		from bookmark_house where user_id = #{userId}
	</select>
	
	<select id="getMyBookmarkHouseOngoingList" parameterType="string" resultType="com.ssafy.happyhouse.dto.BookMarkResultDto">
		select user_id, register_id
		from bookmark_ongoing where user_id = #{userId}
	</select>
	
	<select id="getFriendBookmarkHouseDetailList" parameterType="string" resultType="com.ssafy.happyhouse.dto.BookMarkResultDto">
		select user_id, house_no
		from bookmark_house where user_id = #{userId}
	</select>
	
	<select id="getFriendBookmarkHouseOngoingList" parameterType="string" resultType="com.ssafy.happyhouse.dto.BookMarkResultDto">
		select user_id, register_id
		from bookmark_ongoing where user_id = #{userId}
	</select>
	
	<insert id="insertBookmarkHouseDetail" parameterType="com.ssafy.happyhouse.dto.BookMarkResultDto">
		insert into bookmark_house (user_id, house_no) 
		values ( #{userId}, #{houseNo} )
	</insert>
	
	<insert id="insertBookmarkHouseOngoing" parameterType="com.ssafy.happyhouse.dto.BookMarkResultDto">
		insert into bookmark_ongoing ( user_id, register_id ) 
		values ( #{userId}, #{registerId} )
	</insert>
		
	<delete id="deleteBookmarkHouseDetail" parameterType="com.ssafy.happyhouse.dto.BookMarkResultDto">
		delete from bookmark_house 
		where user_id = #{userId}
		and house_no = #{houseNo}
	</delete>
	
	<delete id="deleteBookmarkHouseOngoing" parameterType="com.ssafy.happyhouse.dto.BookMarkResultDto">
		delete from bookmark_ongoing 
		where user_id = #{userId}
		and register_id = #{registerId}
	</delete>

</mapper>
